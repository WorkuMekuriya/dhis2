package org.dhis2.helper

class Data(
    var Name: String,
    var formName: String,
    var value: String
) {
    // No need for a companion object to define instance properties

    // If you want to define static members, use a companion object like this:
    companion object {}
}